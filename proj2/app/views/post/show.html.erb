<!DOCTYPE html>
<html>
<head>
  <title>Post</title>
</head>
<body>
	<div>
	<h3>Submitted By: <%= link_to @post.user.email, user_path(@post.user) %></h3>
	<h3><%= @post.title %></h3>
	<p><%= @post.content %></p>
	<% if @post.user == current_user %>
		<h4>Something to edit posts here.</h4>
	<% end %>
	</div>
</body>
</html>

