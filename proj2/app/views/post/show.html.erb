<!DOCTYPE html>
<html>
<head>
  <title>Post</title>
</head>
<body>
	<div>
	<h3>Submitted By: <%= link_to @post.user.email, user_path(@post.user) %></h3>
	<h3><%= @post.title %></h3>
	<p><%= @post.content %></p>
	<% @comments.each do |c| %>
		<h3><%= c.user.name %></h3>
		<p><%= c.content %></p>
	<% end %>
	<%= button_to "New Comment", new_comment_path(@post) %>
	</div>
</body>
</html>

