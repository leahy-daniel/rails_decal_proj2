<!DOCTYPE html>
<html>
<head>
  <title>Course</title>
</head>
<body>
<div>
	<span class="glyphicon glyphicon-user" aria-hidden="true"></span>
	<font size='5'><%= @user.name %></font>
	<p><span class="glyphicon glyphicon-flag" aria-hidden="true"></span>
	<font size='5'><%= @user.school_type %></font></p>
	<p><span class="glyphicon glyphicon-bookmark" aria-hidden="true"></span>
	<font size='5'><%= @user.major %></font></p>
	<p><span class="glyphicon glyphicon-triangle-top" aria-hidden="true"></span>
	<font size='5'><%= @user.year %></font></p>
<div class="page-header">
	<h3><%= @user.name %>'s posts:</h3>
		<% @posts.each do |p| %>
			<div class="well">
				<h4>Course name: <%= link_to p.course.name, course_path(p.course) %></h4>
				<h4>Title: <%= link_to p.title, post_path(p) %></h4>
				<p><%= p.content %></p>
			</div>
		<% end %>
	</div>
</body>
</html>

